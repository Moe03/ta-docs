---
title: 'Create KB'
description: "<strong>(VG agents only)</strong> Adds a new document to the agent's knowledge base, if your agent is built with VF use [Voiceflow's API instead](https://developer.voiceflow.com/reference/project). This route must use the new V3 [endpoints](/api-reference/v3/intro)."
openapi: 'POST /agents/{agentId}/kb'
---
